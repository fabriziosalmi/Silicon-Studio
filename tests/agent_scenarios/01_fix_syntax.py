# Test 1: Fix Syntax Error (Easy)
# Prompt: "fix the syntax error in this file"
# Expected: Agent uses patch_file to fix the missing colon and bad indentation

def greet(name)
    msg = f"Hello, {name}!"
    print(msg
    return msg

if __name__ == "__main__":
    greet("World")
